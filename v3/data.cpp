#include <vector>
#include <list>
#include <forward_list>
#include <chrono>
#include <iostream>

class Data_Structure(){
    public:

    void Data_Structure(int type, std::size_t size){
        
    }



    private:
    // The type of the data structure is stored in an integer
    // to simplify code logic. 0 is undefined type, 1 is a vector
    // 2 is a sll, and 3 is a dll
    int type = 0;


}